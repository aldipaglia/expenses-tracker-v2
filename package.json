{
  "name": "PROJECT_NAME_REPLACE_ME",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n BACKEND,UI -c bgBlue.bold,bgGreen.bold -p \"{name}|>\" \"npm:dev -w api\" \"npm:dev -w frontend\""
  },
  "devDependencies": {
    "concurrently": "^7.4.0",
    "typescript": "^4.6.4"
  }
}
